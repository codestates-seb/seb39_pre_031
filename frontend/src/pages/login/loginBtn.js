// import Button from '../../common/Button/Button';
import SubmitButton from '../../common/Button/SubmitButton';

const LoginBtn = ({ ...props }) => {
  return <SubmitButton go="/" {...props} />;
};

export default LoginBtn;
